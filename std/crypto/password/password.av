pckg std.crypto.password;

pub fun hash(password | string) | string {
    return __builtin_crypto_password_hash(password);
}

pub fun verify(password | string, encoded | string) | bool {
    return __builtin_crypto_password_verify(password, encoded);
}
