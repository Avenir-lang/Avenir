pckg main;

import std.time;

async fun delayed_greeting(name | string, delay_ms | int) | string {
    await time.asyncSleep(time.fromMillis(delay_ms));
    return "Hello, " + name + "!";
}

async fun main() | void {
    var greeting | string = await delayed_greeting("World", 50);
    print(greeting);
}
